[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallbacks and redirects live in public/_redirects; unknown URLs get dist/404.html with a real 404.
